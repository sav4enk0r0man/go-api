kind: pipeline
name: default

steps:
- name: build
  image: docker:dind
  volumes:
  - name: dockersock
    path: /var/run/docker.sock
  commands:
  - docker build -t sav4enk0r0man/go-api:dev .

volumes:
- name: dockersock
  host:
    path: /var/run/docker.sock
